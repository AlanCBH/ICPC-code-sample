#include <bits/stdc++.h>
using namespace std;

int main() {
	int num;
	cin >> num;
	for (int a = 0; a < num; a++) {
		int, R, C, M, N,water_num;
		cin >> R >> C >> M >> N;
		cin >> water_num;
		vector<pair<int, int>> water;
		for (int i = 0; i < water_num; i++) {
			int wr, wc;
			cin >> wr >> wc;
			water.push_back(make_pair(wr, wc));
		}
		for () {
		}
	}
}