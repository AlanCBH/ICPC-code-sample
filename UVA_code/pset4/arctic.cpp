#include <bits/stdc++.h>
using namespace std;
int dist[500][500];
int main() {
	int num,x,y,snum,onum;
	cin >> num;
	for (int a = 0; a < num; a++) {
		cin >> snum >> onum;
		for (int i = 0; i < onum;i++) {
			cin >> x >> y;
		}
	}
	return 0;
}